import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 20,
  },
  mensaje: {
    marginTop: 40,
    textAlign: 'center',
    fontSize: 16,
    color: '#666',
  },
});
